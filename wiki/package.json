{
  "name": "wiki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "type-check": "pnpm -w exec tsc --noEmit -p wiki",
    "lint": "pnpm -w exec eslint wiki --cache",
    "lint:fix": "pnpm -w exec eslint wiki --fix --cache",
    "format": "pnpm -w exec prettier -w wiki",
    "format:check": "pnpm -w exec prettier -c wiki"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vue": "^3.5.26",
    "typescript": "~5.9.3",
    "@types/node": "^24.10.4",
    "vitepress": "^1.6.4"
  }
}
